#include<stdio.h>
#include<stdlib.h>

typedef struct node{
	int val;
	struct node * lChild;
	struct node * rChild;
}bTree;

const int sizeOfTree = sizeof(bTree);

bTree * root = NULL;

int main(){

	return 0;
}